#using package.json
cd to directory
/opt/node/bin/npm install
/opt/node/bin/node server.js

_____________________-

# install npm, git
apt-get install npm
apt-get install git

#install nvm, node
sudo mkdir /opt/node
wget http://nodejs.org/dist/v0.11.10/node-v0.11.10-linux-arm-pi.tar.gz
tar xvzf node-v0.11.10-linux-arm-pi.tar.gz
sudo cp -r node-v0.11.10-linux-arm-pi/* /opt/node
rm -rf node-v0.11.10-linux-arm-pi
rm -rf node-v0.11.10-linux-arm-pi.tar.gz


npm install ds18b20
npm install socket.io
npm install express

nano /etc/profile
# add these lines before export PATH
NODE_JS_HOME="/opt/node"
PATH="$PATH:$NODE_JS_HOME/bin"

logout


#add nodejs.sh

chmod 755 nodejs.sh
sudo cp nodejs.sh /etc/init.d
sudo update-rc.d nodejs.sh defaults

sudo /etc/init.d/nodejs.sh start

#git checkin
git add .
git commit -m "push power state"
git push -u origin master

#git checkout
cd github.com/goossen
rm -rf reef
git clone git://github.com/goossen/reef.git

sudo /etc/init.d/nodejs.sh stop
sudo /etc/init.d/nodejs.sh start







